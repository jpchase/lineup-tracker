// console.log(`mock indexedDB, previously: ${typeof indexedDB}`);
// var indexedDB = require('fake-indexeddb');
// console.log(`Now have: ${typeof indexedDB}`);

console.log(`mock firebase, previously: ${typeof firebase}`);
var firebase = require('firebase');
console.log(`Now have: ${typeof firebase}`);
